# first tab with four terminals laid out using grid

enabled_layouts grid, stack, splits
layout grid

cd ~/projects/lightspeed-spark
launch
focus
launch
launch
launch

# second tab using the generic splits layout
new_tab
enabled_layouts splits, stack
layout splits

cd ~/projects/ybox
launch
launch
